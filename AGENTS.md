# 任务管理课堂项目

- 技术栈为 Vue 3、Vite、Tailwind CSS、JavaScript、localStorage，无后端。
- 原始课件位于父目录，不修改课程资料。
- 功能以《课堂实践.pptx》为准，不加入登录或云同步。
- 任务状态和存储逻辑集中管理；组件分为表单、卡片、列表与看板。
- 每轮实现后执行相应验证，再提交相关文件。提交前运行 `git diff --check`。
- 交付前运行 `npm test`、`npm run build`，并在浏览器验证主要流程。
